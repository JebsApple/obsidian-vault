# Sub-Plan F1.2: Telemetría Per-Unit

**Agente**: Sub-agente B
**Dificultad**: MUY BAJA | **Archivos**: 1–2 | **Estimación**: 0.5h

## Objetivo
Verificar que la telemetría ya calcula `costo_por_unidad` (ms por globo, ms por página) y que el script `scripts/telemetria.mjs` lo reporta.

## Contexto
- `server/telemetry/store.ts` ya tiene las queries de `msPorGlobo` y `msPorPagina` en `obtenerResumen()` (líneas ~104-130)
- Usa `percentile_disc(0.5)` sobre el cociente `duracion_ms / cantidadGlobos`

## Archivos

### 1. `scripts/telemetria.mjs`
Verificar que el script de consola muestre las columnas `ms/globo` y `ms/página`. Si no:

```javascript
// Agregar al reporte:
// ms_por_globo: row.msPorGlobo ?? '—'
// ms_por_pagina: row.msPorPagina ?? '—'
```

### 2. `server/telemetry/store.ts` (solo lectura de verificación)
Confirmar que `obtenerResumen()` ya incluye `msPorGlobo` y `msPorPagina` en su return.

## Skills a cargar
- Ninguna específica, es una verificación

## Verificación
- [ ] `npm run telemetria` muestra `ms/globo` en el reporte
- [ ] Los valores son numéricos, no null
- [ ] `npm test` pasa
