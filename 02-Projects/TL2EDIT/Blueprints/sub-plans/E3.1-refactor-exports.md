# Sub-Plan E3.1: Refactor Exports (PR #87)

**Agente**: Sub-agente específico
**Dificultad**: ALTA | **Archivos**: ~6 (1 nuevo directorio, 4 nuevos archivos, 2 modificar imports) | **Estimación**: 6–10h

## CRÍTICO: Refactor que NO debe cambiar comportamiento. Estrategia: snapshot test PRE, refactor, comparar POST.

## Objetivo
Separar `src/lib/googleDrive.ts` (469 líneas) en una estructura por capas:
- `api.ts`: solo HTTP calls a Drive API
- `folders.ts`: lógica de carpetas
- `naming.ts`: convenciones de nombres
- `types.ts`: tipos

Neto esperado: -230 líneas (de 469 → ~240).

## Proceso (TDD para refactor seguro)

### Paso 1: Snapshot test PRE-refactor
Crear un test que exporte un DOCX y compare el snapshot binario:
```typescript
// src/lib/googleDrive.refactor.test.ts (temporal)
import { describe, it, expect } from 'vitest'

describe('googleDrive refactor pre-check', () => {
  it('saveSessionToDrive firma no cambia', () => {
    // Verificar que las funciones exportadas existen con las firmas esperadas
  })
})
```

### Paso 2: Crear `src/lib/drive/types.ts`
Mover tipos desde `googleDrive.ts`:
```typescript
export interface DriveSessionFile {
  id: string
  name: string
  modifiedTime: string
}

export interface DriveExportResult {
  id: string
  webViewLink: string
}

export interface SessionPageRef {
  id: string
  fileName: string
  imageFileId: string
  driveOwned: boolean
  mimeType: string
  width: number
  height: number
  status: string
  textBlocks: unknown[]
  cutsPx?: number[]
}

export interface SessionManifest {
  version: number
  savedAt: number
  activePageId: string | null
  pages: SessionPageRef[]
}

export interface SessionPageInput {
  id: string
  fileName: string
  base64Data: string
  mimeType: string
  width: number
  height: number
  status: string
  textBlocks: unknown[]
  cutsPx?: number[]
  driveFileId?: string
  driveOwned?: boolean
}

export interface DriveItem {
  id: string
  name: string
  isFolder: boolean
  isArchive?: boolean
  thumbnailLink?: string
}

export class DriveCancelledError extends Error {
  constructor() {
    super('Operación cancelada.')
    this.name = 'DriveCancelledError'
  }
}
```

### Paso 3: Crear `src/lib/drive/naming.ts`
```typescript
export const SESSIONS_FOLDER_NAME = 'TL2Edit Sessions'
export const SESSION_FILE_SUFFIX = '.tl2edit.json'
export const EXPORTS_FOLDER_NAME = 'TL2Edit Exportaciones'
export const SESSION_FORMAT_VERSION = 1
```

### Paso 4: Crear `src/lib/drive/api.ts`
Solo HTTP calls:
- `driveFetch()` con timeout
- `driveFetchWithRetry()` con backoff (⚠️ función creada en 2.3, migrarla aquí)
- `dataUrlToBlob()` (⚠️ función privada actual en googleDrive.ts, moverla)
- `uploadJsonToDrive()`
- `uploadImageToDrive()`
- `uploadBufferToDrive()`
- `downloadDriveImage()`
- `convertToGoogleDoc()`
- `uploadExportFile()`
- `uploadTextAsGoogleDoc()`

### Paso 5: Crear `src/lib/drive/folders.ts`
Solo lógica de carpetas:
- `ensureSessionsFolder()`
- `ensureExportsFolder()`
- `createDriveFolder()`
- `attachFileToFolder()` (⚠️ función privada actual en googleDrive.ts, moverla)
- `listDriveFolder()`
- `listDriveSessions()`
- `saveSessionToDrive()`
- `loadSessionFromDrive()`
- `listExportFilesInFolder()` (nueva, para overwrite)

### Paso 6: Re-exportar desde `src/lib/googleDrive.ts`
```typescript
export * from './drive/types'
export * from './drive/naming'
export * from './drive/api'
export * from './drive/folders'
```
Esto mantiene compatibilidad con imports existentes.

### Paso 7: Snapshot test POST-refactor
Verificar que el output binario es idéntico al PRE-refactor.

## ⚠️ Orden de ejecución
**2.3 debe ejecutarse ANTES que E3.1**. 2.3 crea `driveFetchWithRetry()` en `googleDrive.ts`. E3.1 debe migrar esa función a `api.ts`. Si E3.1 se ejecuta antes, `driveFetchWithRetry()` no existe.

## Skills a cargar
- `ecc-rules` (inmutabilidad, testing, DRY)
- `karpathy-guidelines` (surgical changes: cero cambio de comportamiento)

## Archivos a NO tocar
- `src/hooks/useExport.ts` → solo actualizar imports (el barrel export mantiene compatibilidad)
- `src/hooks/useGoogleAuth.ts` → solo actualizar imports
- `server/lib/googleAuth.ts` → NO TOCAR

## Verificación
- [ ] `googleDrive.ts` sigue exportando las mismas funciones (compatibilidad)
- [ ] `npm test` pasa (todos los tests existentes siguen funcionando)
- [ ] `npm run build` pasa
- [ ] Output binario PRE = POST (idempotencia)
