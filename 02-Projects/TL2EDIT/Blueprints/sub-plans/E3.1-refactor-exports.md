# Sub-Plan E3.1: Refactor Exports (PR #87)

**Agente**: Sub-agente específico
**Dificultad**: ALTA | **Archivos**: ~6 | **Estimación**: 6–10h

> **Al ejecutor**: El código sugerido es una guía, no un mandato. Si encuentras una mejor solución, critícala y mejórala. Si algo no está claro o falta contexto, PREGUNTA.
> **CRÍTICO**: Refactor que NO debe cambiar comportamiento. Estrategia: snapshot test PRE, refactor, comparar POST.

## 🥩 Grilling (preguntas que resolver antes de empezar)

1. **Colisiones en barrel export**: `export * from './drive/api'` + `export * from './drive/types'` — si ambos exportan `DriveCancelledError`, hay error en tiempo de compilación o runtime. Verificar nombres únicos entre los 4 archivos destino.

2. **Funciones privadas en googleDrive.ts**: Identificar TODAS las funciones que NO están exportadas (`dataUrlToBlob`, `driveFetch`, `escapeName`, etc.) — migrarlas a `api.ts` o mantenerlas privadas en el barrel.

3. **¿Qué test de snapshot usar?** Snapshot de output DOCX binario no es humano-lecturable. Alternativa: test de integración que verifica que:
   - `saveSessionToDrive` tiene la misma firma
   - `convertToGoogleDoc` acepta los mismos parámetros
   - `listDriveSessions` devuelve el mismo tipo

4. **Orden**: 2.3 (drive hardening) DEBE ejecutarse antes que E3.1 porque E3.1 migrará `driveFetchWithRetry()`.

## Orden de ejecución
1. ✅ **2.3 Drive hardening** (crea `driveFetchWithRetry()` en googleDrive.ts)
2. **E3.1 Refactor** (mueve `driveFetchWithRetry()` a `api.ts`)

## Objetivo
Separar `src/lib/googleDrive.ts` (469 líneas) en estructura por capas.

## Archivos

### 1. `src/lib/drive/types.ts`
Tipos: `DriveSessionFile`, `DriveExportResult`, `SessionPageRef`, `SessionManifest`, `SessionPageInput`, `DriveItem`, `DriveCancelledError`.

### 2. `src/lib/drive/naming.ts`
Constantes: `SESSIONS_FOLDER_NAME`, `SESSION_FILE_SUFFIX`, `EXPORTS_FOLDER_NAME`, `SESSION_FORMAT_VERSION`.

### 3. `src/lib/drive/api.ts`
Solo HTTP calls:
- `driveFetch()` (con timeout + abort)
- `driveFetchWithRetry()` (migrada desde 2.3)
- `dataUrlToBlob()`
- `uploadJsonToDrive()`, `uploadImageToDrive()`, `uploadBufferToDrive()`
- `downloadDriveImage()`
- `convertToGoogleDoc()`, `uploadExportFile()`, `uploadTextAsGoogleDoc()`
- `overwriteExportFile()` (nueva)

### 4. `src/lib/drive/folders.ts`
Lógica de carpetas:
- `ensureSessionsFolder()`, `ensureExportsFolder()`
- `createDriveFolder()`, `attachFileToFolder()`
- `listDriveFolder()`, `listDriveSessions()`
- `saveSessionToDrive()`, `loadSessionFromDrive()`
- `listExportFilesInFolder()` (nueva)

### 5. `src/lib/googleDrive.ts` — barrel export
```typescript
export * from './drive/types'
export * from './drive/naming'
export * from './drive/api'
export * from './drive/folders'
```
Verificar que no haya colisiones de nombres entre los 4 archivos.

## Skills a cargar
- `ecc-rules` (DRY, single responsibility)
- `karpathy-guidelines` (cero cambio de comportamiento)

## Verificación
- [ ] googleDrive.ts sigue exportando las mismas funciones
- [ ] No hay colisiones de nombres en barrel export
- [ ] `npm test` pasa (tests existentes funcionan con imports actualizados o barrel)
- [ ] `npm run build` pasa
