# ~~Sub-Plan F1.6: Integrar Apodos en Exportación DOCX~~ OBSOLETO

## ❌ Este sub-plan está OBSOLETO

**Motivo**: El sub-plan **2.1.1** (nuevo formato DOCX) ya reescribe `exportDocx.ts` completo e integra `applyNicknames()` directamente. F1.6 proponía cambios sobre la estructura vieja (con `buildPageParagraphs`/`groupId`/`flushLine`), que 2.1.1 elimina por completo.

**Si se ejecuta**: El código de F1.6 sería pisado por 2.1.1 (o viceversa), causando conflicto de merge.

**Acción**: La integración de apodos en exportación está CUBIERTA por 2.1.1. No ejecutar F1.6.
