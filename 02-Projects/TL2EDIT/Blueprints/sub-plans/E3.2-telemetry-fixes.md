# Sub-Plan E3.2: Telemetría 3.1.4 Fixes

**Agente**: Sub-agente específico
**Dificultad**: MEDIA | **Archivos**: ~14 | **Estimación**: 6–10h

> **Al ejecutor**: El código sugerido es una guía, no un mandato. Si encuentras una mejor solución, critícala y mejórala. Si algo no está claro o falta contexto, PREGUNTA.

## 🥩 Grilling (preguntas que resolver antes de empezar)

1. **Cada defecto = 1 commit con test**: ¿Todos los defectos son reproducible? Algunos P2 son visuales (padding) — no se puede testear automáticamente. Marcar esos como "verificación manual".

2. **P0-1 y P0-2 están cubiertos por 2.3 y 2.1.1**: Marcar como DEPENDIENTES. Si 2.3 o 2.1.1 no se ejecutan, estos P0s pueden persistir.

## Proceso
1 defecto = 1 commit. Cada commit debe incluir el test que reproduce el bug (si el bug es testable automáticamente).

### P0 (Bloqueantes)

| ID | Defecto | Archivo | Fix | Test |
|----|---------|---------|-----|------|
| P0-1 | Error 400 en Drive con `orderBy` | `googleDrive.ts` | ✅ cubierto en 2.3.2 | Mock que verifica URL encode |
| P0-2 | DOCX multi-página no separa | `exportDocx.ts` | ✅ cubierto en 2.1.1 | Test con 3 páginas mock |
| P0-3 | Tildes rotas en UI | Varios | Revisar encoding UTF-8 | Visual |

### P1 (Graves)

| ID | Defecto | Archivo | Fix sugerido | Test |
|----|---------|---------|-------------|------|
| P1-1 | Bloques imagen/nota no renderizan | `ComicCanvasView.tsx` | Verificar que `blockType === 'imagen'` renderiza correctamente en el canvas | Render test con bloque imagen mock |
| P1-2 | Scroll sidebar pierde posición | `SidebarTextBlocks.tsx` | Guardar `scrollTop` en un ref antes de cambiar página, restaurar tras render | Test con scroll + cambio de página |
| P1-3 | Error silencioso sin accessToken | `useExport.ts` | Al inicio de export: `if (!accessToken) throw new Error('No hay sesión de Google Drive')` | Test: exportar sin token → error visible |

### P2 (Molestos)

| ID | Defecto | Archivo | Fix | Test |
|----|---------|---------|-----|------|
| P2-1 | DriveFolderPicker no refresca | `DriveFolderPicker.tsx` | Forzar `setState` después de crear carpeta | Visual |
| P2-2 | Evento duplicado en cancelación | `telemetry/client.ts` | Verificar lógica de cancelación | Unit test de evento cancelado |
| P2-3 | Padding inconsistente en botones | `index.css` | Unificar variables CSS de padding de botones | Visual |

## Verificación
- [ ] Cada defecto P0 tiene test que lo reproduce (excepto visuales)
- [ ] `npm test` pasa
- [ ] `npm run build` pasa
