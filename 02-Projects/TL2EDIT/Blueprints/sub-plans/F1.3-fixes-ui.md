# Sub-Plan F1.3: Fixes UI Puros (PR #86)

**Agente**: Sub-agente C
**Dificultad**: BAJA | **Archivos**: 2 | **Estimación**: 1h

> **Al ejecutor**: El código sugerido es una guía, no un mandato. Si encuentras una mejor solución, critícala y mejórala. Si algo no está claro o falta contexto, PREGUNTA.

## 🥩 Grilling (preguntas que resolver antes de empezar)

1. **`* { scrollbar-width: thin }` es global** — ¿Afecta a TODOS los scrollbars de la app, incluyendo textareas, selects, y el canvas? Verificar visualmente que no rompe la UI de componentes que esperan scrollbar normal.

2. **¿Dónde exactamente está `calc(100%-32px)`?** Buscar en todo el proyecto (no solo App.tsx). Puede haber múltiples instancias. Reemplazar TODAS con `calc(100% - 32px)`.

3. **`DataTransfer.setDragImage()`** — ¿Dónde está el handler `dragstart` actual? Buscar `onDragStart` o `dragStart` en los componentes del canvas. No crear helper nuevo, solo agregar 3 líneas donde ya existe el handler.

## Objetivo
Corregir 3 bugs visuales en la UI: scrollbar-corner, Firefox scrollbar-color, y Toast calc.

## Archivos a modificar

### 1. `src/index.css`
Agregar al final del archivo:

```css
/* Scrollbar-corner transparente (Chrome/Edge/Safari) */
::-webkit-scrollbar-corner {
  background: transparent;
}

/* Firefox scrollbar-color */
* {
  scrollbar-width: thin;
  scrollbar-color: var(--bg) transparent;
}
```

⚠️ **Verificar**: El selector global `*` afecta a TODOS los elementos. Si hay textareas o selects que necesitan scrollbar de ancho normal, usar una clase más específica (ej: `.main-scrollbar`).

### 2. `src/App.tsx` — Toast `calc()` bug
Buscar `calc(100%-32px)` y reemplazar por `calc(100% - 32px)`.
REEMPLAZAR EN TODO EL PROYECTO: `grep -r "calc(100%-" src/`

### 3. NO crear `src/lib/dragPreview.ts`
No se necesita helper. Usar `DataTransfer.setDragImage()` donde ya existe el manejador de drag.

## Skills a cargar
- `karpathy-guidelines` (surgical changes: 1 línea cada fix)

## Tests
- Verificar visualmente que:
  - Scrollbar-corner no muestra fondo blanco
  - Firefox: scrollbar usa colores del tema
  - Toast no se desborda del contenedor
- `npm test` pasa

## Verificación
- [ ] Scrollbar-corner transparente en Chrome/Edge
- [ ] Firefox scrollbar-color coincide con tema
- [ ] Toast con `calc(100% - 32px)` funciona correctamente
- [ ] `* { scrollbar-width: thin }` no rompe textareas ni selects
- [ ] `npm test` pasa
