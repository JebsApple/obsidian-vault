# Sub-Plan F1.3: Fixes UI Puros (PR #86)

**Agente**: Sub-agente C
**Dificultad**: BAJA | **Archivos**: 2 | **Estimación**: 1h

## Objetivo
Corregir 3 bugs visuales en la UI: scrollbar-corner, Firefox scrollbar-color, y Toast calc.

## Archivos a modificar

### 1. `src/index.css`
Agregar al final del archivo:

```css
/* Scrollbar-corner transparente */
::-webkit-scrollbar-corner {
  background: transparent;
}

/* Firefox scrollbar-color */
* {
  scrollbar-width: thin;
  scrollbar-color: var(--bg) transparent;
}
```

### 2. Toast `calc()` bug
Buscar en `src/App.tsx` la línea:
```css
calc(100%-32px)
```
Reemplazar por:
```css
calc(100% - 32px)
```
(Faltaban espacios alrededor del operador)

### 3. NO crear `src/lib/dragPreview.ts`
No se necesita helper de drag preview. Usar la API nativa `DataTransfer.setDragImage()` donde sea necesario.

## Skills a cargar
- `karpathy-guidelines` (surgical changes: 1 línea cada fix)

## Tests
- Verificar visualmente que:
  - Scrollbar-corner no muestra fondo blanco
  - Firefox: scrollbar usa colores del tema
  - Toast no se desborda del contenedor
- `npm test` pasa

## Verificación
- [ ] Scrollbar-corner transparente en Chrome/Edge
- [ ] Firefox scrollbar-color coincide con tema
- [ ] Toast con `calc(100% - 32px)` funciona correctamente
- [ ] `npm test` pasa
