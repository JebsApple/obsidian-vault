---
fecha: 2026-07-01 15:35:36
modo: plan
dir: /home/icin
exit: 0
---
# 🤖 Respuesta IA — 20260701-153501

## Pedido
```
dame el status del proyecto

Para lanzar: cambiá `estado: pendiente` por `estado: ejecutar`, guardá y hacé Push.
La respuesta aparece en IA-Control/respuestas/ en unos 2-4 min (hacé Pull para verla).
```

## Resultado

Ignoring 41 permissions.allow entries from .claude/settings.local.json: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog, or set projects["/home/icin"].hasTrustDialogAccepted: true in /home/icin/.claude.json.
## Estado del proyecto MiNegocio (al 2026-06-30, última actualización del vault)

**Stack:** Vue.js 3 (frontend, `minegocio-frontend/`) + Go REST (backend, `minegocio-backend/`, puerto 3000) + PostgreSQL 16 (`minegocio-database/`).

**Frontend — rama integradora `S3-HU02`:** al día, contiene T13 (fusión de vistas + cropper) y T14 (sidebar colapsable + pestañas estilo archivador + fix POS). Además se hicieron 2-3 iteraciones de refinamiento UI (T15) sobre tabs y sidebar, deployadas a DEV y verificadas en el bundle CSS.

Ramas pusheadas a Gitea pendientes de revisión/merge: T01 (nav sidebar), T02 (kanban), T03 (panel principal), T04 (pruebas), T05 (validaciones), T06 (capas frontend), T07 (kanban inventario), T08 (ajustes estilo), T09 (nombres español), T11 (íconos), T12 (código de barras + roles), T13 (fusión vistas). `main` sigue en v3.0.0 esperando el merge de S3-HU02 para v3.1.0. Existe rama `dev` para testeo del equipo.

**Backend:** `main` estable con JWT + API de ventas + CRUD productos. Ramas abiertas de Gabriel: bcrypt passwords (T01), JWT_SECRET por entorno (T02), y `feat/S3-HU02` con endpoint kanban.

**Database:** `main` con esquema base; rama T13 tiene esquema completo + seed de usuarios DEV + migraciones.

**Otros temas activos recientes:**
- Sandbox aislado de OpenCode para Nicolás Valdés configurado y verificado (comando `nico`, push deshabilitado, Gitea read-only).
- ⚠️ Se detectó una contraseña de la DB dev expuesta en texto plano en documentación — pendiente de revisar/rotar.
- Sync del vault Obsidian confirmado operativo en ambas direcciones (push automático vía cron cada 5 min).
- Disco del servidor al 93% en un momento reciente por caché de builds de Docker — vigilar espacio.

¿Querés que profundice en algo puntual (ej. detalle de una tarea, el bug de la contraseña expuesta, o el estado de un PR específico)?
